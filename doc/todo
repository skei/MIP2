

//----------------------------------------------------------------------
//
// audio
//
//----------------------------------------------------------------------

* voice manager

  new voice manager class

* tick processing

  tick = 16 floats = 64 butes = cache-line = 4 simd
  hardcode 16-sample dsp modules/loops,
  (with additional n-sample to fill out missed samples)
  events inbetween, jitter max 15 samples, 0.3ms

//----------------------------------------------------------------------
//
// base
//
//----------------------------------------------------------------------

* win32 support

  - plugin entry points. (dll)
  - gui (using gdi/msimg32): window, surface, painter
  - other: MIP_Attributes, MIP_Time, ..

//----------------------------------------------------------------------
//
// gui
//
//----------------------------------------------------------------------

* clipping/hierarchy

  examine clipping and widget hierarchy
  push/pop clip rectangles

  topmost (window) cliprect vs resizing

* combine update rects in expose events (xcb gui)?
  #ifdef MIP_XCB_COMBINE_EXPOSE_RECTS

  many small rectangles (blinking lights) -> 1 on_window_paint() call
  but unnecessary redrawing for unchanged 'inbetweeen' widgets

//----------------------------------------------------------------------
//
// midi
//
//----------------------------------------------------------------------

* midi player
  prepare midi events for clap/vst2/vst3?

//----------------------------------------------------------------------
//
// plugin
//
//----------------------------------------------------------------------

* resizable editor
